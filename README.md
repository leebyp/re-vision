Re-Vision
======

Simple ionic project to create revision style questions.

Quick start
------

Install ionic:
  ```
    $ npm install -g ionic
  ```

Desktop browser testing:
  ```
    $ ionic serve
  ```

iOS simulator testing:
  ```
    $ ionic build ios
    $ ionic emulate ios
  ```
